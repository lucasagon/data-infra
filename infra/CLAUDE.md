Você precisa sempre realizar commits e push após realizar alterações no projeto e também atualizar o arquivo projects_memory.md com o snapshot de todo o detalhamento atual do projeto. Antes de realizar os commits e push e após fazer as alterações, ler o projects_memory para verificar o estado atual e alterar nele o que for pertinente de acordo com o que foi alterado. caso não encontre nada, ajustar o arquivo para o novo item mantendo os demais, sempre sera um snapshop do estado atual. Você precisa ser rigoroso com essas diretrizes e sempre executar elas, em cada interação com o usuário via prompt. caso voc~e ainda não saiba o repositório git e a branch do commit e push, peça para o usuário lhe informar ambos e também se irá logar via http ou ssh no github.
